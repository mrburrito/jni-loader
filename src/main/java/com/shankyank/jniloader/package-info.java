/**
 * Provides canonicalization of operating system and architecture names from the
 * System properties <code>os.name</code> and <code>os.arch</code> and dynamic
 * loading of native libraries from bundled resources.
 */
package com.shankyank.jniloader;